<div class="container">
  <h3>Articles</h3>

  <div class="ml-3 align-items-center">
    <%= link_to "➕ Add article", new_article_path %>
  </div>

  <% @articles.each do |article|%>
    <div class="shadow p-4 m-4 bg-light rounded align-items-center">
      <h5> <%= link_to article.title, article %> </h5> <br>
      <p> <%= article.content %> </p>
    </div>
  <% end %>
</div>
